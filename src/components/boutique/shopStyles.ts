import { BRAND_YELLOW } from '../../constants/brand';
import { StyleSheet } from 'react-native';
export const shopColors = { gold: BRAND_YELLOW, ink: '#171C11', background: '#10130F', panel: '#1B2216', border: '#333E29', text: '#F3F3E9', muted: '#B0BCA2' };
const c = shopColors;
export const money = (value: number) => value.toLocaleString('fr-FR', { maximumFractionDigits: 2, minimumFractionDigits: 2 });
export const shopStyles = StyleSheet.create({
  root: { flex: 1, backgroundColor: c.background }, header: { minHeight: 64, flexDirection: 'row', alignItems: 'center', gap: 12, paddingHorizontal: 20 }, iconButton: { width: 44, height: 44, borderRadius: 14, alignItems: 'center', justifyContent: 'center' }, headerTitle: { flex: 1, fontSize: 16, fontWeight: '600', color: c.text, textAlign: 'center' }, content: { padding: 20, paddingBottom: 28 }, eyebrow: { fontSize: 10, letterSpacing: 1.6, fontWeight: '700', color: c.gold, textTransform: 'uppercase' }, title: { fontSize: 28, lineHeight: 35, fontWeight: '700', letterSpacing: -0.8, color: c.text, marginTop: 10, marginBottom: 10 }, caption: { fontSize: 13, lineHeight: 20, color: c.muted }, panel: { padding: 18, borderWidth: 1, borderColor: c.border, borderRadius: 20, backgroundColor: c.panel, marginTop: 14 }, sectionTitle: { fontSize: 16, fontWeight: '600', color: c.text }, row: { flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between', gap: 12, marginTop: 12 }, value: { fontSize: 13, color: c.text, fontWeight: '600' }, total: { fontSize: 24, fontWeight: '700', color: c.gold }, footer: { paddingHorizontal: 20, paddingTop: 2, borderTopWidth: 1, borderColor: c.border, backgroundColor: '#181F13' }, primary: { minHeight: 54, borderRadius: 15, backgroundColor: c.gold, paddingHorizontal: 20, paddingVertical: 15, flexDirection: 'row', alignItems: 'center', justifyContent: 'center', gap: 10, marginTop: 16 }, primaryText: { color: c.ink, fontSize: 15, fontWeight: '700', flexShrink: 1, textAlign: 'center' }, input: { minHeight: 54, borderWidth: 1, borderColor: '#48553B', backgroundColor: '#12180E', borderRadius: 13, color: c.text, paddingHorizontal: 14, paddingVertical: 13, fontSize: 16 }, error: { fontSize: 12, color: '#F3AB9F', marginTop: 8, lineHeight: 18 }, disabled: { opacity: 0.5 }, empty: { flex: 1, alignItems: 'center', justifyContent: 'center', padding: 30, gap: 16 },
});
